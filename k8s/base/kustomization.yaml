# =============================================================================
# ARED Edge Substrate Node - Kustomize Base
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ared-edge

commonLabels:
  app.kubernetes.io/part-of: ared-edge-iot-platform
  app.kubernetes.io/managed-by: kustomize

resources:
  - statefulset.yaml

configMapGenerator:
  - name: substrate-config
    literals:
      - CHAIN=production
      - NODE_NAME=ared-edge-validator
